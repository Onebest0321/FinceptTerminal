name: Translate README

on:
  push:
    paths:
      - 'README.md'

jobs:
  translate:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        lang: [es, zh-CN, ja, fr]   
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Translate README to ${{ matrix.lang }}
        uses: dephraiim/translate-readme@main
        with:
          LANG: ${{ matrix.lang }}
